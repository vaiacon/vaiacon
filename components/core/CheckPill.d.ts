/** Pill-Chip mit Terracotta-✓ für kurze Vertrauensargumente. Rendert ein <li> — in <ul> mit flex + gap 12px setzen. */
export interface CheckPillProps {
  style?: React.CSSProperties;
  children?: React.ReactNode;
}
